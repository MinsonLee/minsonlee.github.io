- 查询所有 `markdown` 文件下，通过有道云自身存储的图片

```bash
grep -Ern "\!\[.*\]\([0-9a-zA-Z]+\)" --color
```

- 查询所有的非 `*.md` 文件

```bash
find -type f ! -name ".*md"
```

- 查询所有非 `.*md` 文件的类型

```bash
find -type f ! -name "*.md" | rev | cut -f 1 -d '.' | sort | uniq | rev
```
